# ファイル名: config.R
# 役割: プロジェクト全体の設定を管理する

# ---------------------------------------------------------------------------- #
# 【要件】入力CSVファイルの構造
# 1. 1行目はヘッダー（列名）であること。
# 2. 目的変数(y)と説明変数(ex)の列が含まれていること。
#    - 下の「入力ファイルと列名の設定」で、対応する列名を指定してください。
# 3. (任意) 時間軸を表す列（日付など）を含めることができます。
#    - この列がない場合、横軸は 1, 2, 3, ... というインデックスになります。
# ---------------------------------------------------------------------------- #

library(tidyverse) # このプロジェクトで共通して使うパッケージはここで読み込んでも良い

# ---- 1. 入力ファイルと列名の設定 ----
CSV_FILE_PATH <- "sample_sales_data.csv" # 分析したいCSVファイルのパス
Y_COL_NAME    <- "sales"           # 目的変数(y)の列名
EX_COL_NAME   <- "promotion"           # 説明変数(ex)の列名
TIME_COL_NAME <- "date"           # (任意)時間軸の列名。なければ NULL を指定
# TIME_COL_NAME <- NULL           # 時間軸がない場合はこちらを有効化

# ---- 2. モデルとMCMCの設定 ----
STAN_MODEL_FILE <- "model_tvcm.stan"
CHAINS <- 4
ITER   <- 8000
WARMUP <- 2000
THIN   <- 6
SEED   <- 1


# ---- 3. 出力ファイル名の設定 ----
# 推定結果（stanfitオブジェクト）の保存先
OUTPUT_RDS_FILE  <- "tvcm_model_fit.rds"
# 最終的なグラフの保存先
PLOT_OUTPUT_FILE <- "tvcm_plot_results.png"

# ---- 4. グラフデザインの設定 ----
PLOT_WIDTH  <- 12
PLOT_HEIGHT <- 10
PLOT_DPI    <- 300

